---
name: xhs-makeup-mimic
description: 小红书化妆师笔记生成器——把"日期 / 妆容想法 / 参考图片"自动产出一整套发布素材：3 张 2:3 同一人物的 JSON 中文图片提示词（面向 GPT-image-2 / Nano Banana 2）+ 3 个版本的标题 + 3 个版本的正文。基于"花里胡哨的小搓澡巾"博主 4245 赞爆款 DNA，落地到"都匀桃九九化妆室"模仿账号。务必在用户提到「小红书笔记 / 化妆师笔记 / 妆容笔记 / 写小红书 / 发笔记 / 帮我想个标题 / 帮我写文案 / 小红书出图 / 化妆师 IP / 都匀化妆师 / 桃九九化妆室 / 今天发什么 / 妆容图 / 客返图 / 约妆笔记 / GPT-image-2 / nano banana / Gemini 出图」等场景时触发；即便用户只说"今天要发一条"、"我有这张妆容图想发"、"出 3 张图"、"想个文案"，也要立即调用本 skill。
---

# 小红书化妆师笔记生成器 (xhs-makeup-mimic)

## 你的工作

把用户给你的「日期 / 想法 / 参考图」三类输入之一（或组合），转换成可直接发布的小红书笔记三件套：

1. **3 张图的提示词**——JSON 结构化中文，面向 GPT-image-2 / Nano Banana 2；必须是同一个人物，2:3 比例（小红书最佳）
2. **3 个版本的标题**——分别套用爆款博主验证过的 A/C/D 公式
3. **3 个版本的正文**——80字精简 / 120字标准 / 150字深度

## 调用条件 — 何时启动这个流程

只要用户表达"我想发一条小红书笔记"的任何变体——无论是给你日期、给你妆容图片，还是只说一句模糊的想法——立即按本 skill 的 8 步法执行。不要追问"你想怎么样"，先用合理默认值出一版，再让用户调整。

## 核心原则（**勿动**）

> 这些约束来自对原博主 292 条笔记 + 4 篇爆款（4245/2210/1176/1082 赞）的统计分析，是经过验证的爆款 DNA。

1. **人物一致性是生命线**——三张图必须看起来是同一个人。这是用户最在意的硬性要求，没有商量余地。优先级高于任何美学诉求。
2. **地域锚是"都匀"**——模仿账号在贵州黔南都匀。原博主的"南京"只作风格参照，绝不出现在输出里。
3. **正文必须 80 字起，反原博主短板**——原博主 100% 笔记正文是 hashtag 堆砌，是其最大缺点。模仿账号必须写真实化叙事（客户故事 / 技术解读 / 价格信号）。
4. **标题严格 12-22 字**——小红书后台规则上限 20 字，超出会被截断。
5. **2:3 比例**——小红书图文最佳显示比例，必须出现在每张图提示词里。

## 8 步生成法

### 第 1 步：接收并归类输入

用户输入可能是以下任意一种或组合：

| 输入类型 | 处理方式 |
|---|---|
| **只有日期**（如"2026-05-13"/"明天"/"周末"） | 推导季节/节日/场景钩子（春日 / 演唱会档期 / 母亲节 / 毕业季 / 七夕…） |
| **只有想法**（如"想发一个韩系妆"） | 直接进入主题分类（A/B/C/D/E 五选一） |
| **只有参考图** | 用 Read 工具读图 → 提取人物特征卡 + 妆面元素卡 |
| **想法 + 参考图** | 综合：图作人物锚，想法作主题 |
| **想法 + 日期** | 综合：日期作节日钩子，想法作主题 |

如果只有日期但没有任何主题线索，**默认选主题 A（妆面展示）+ 风格主线为「日杂」或「韩系亚裔」**——这是原博主爆款占比最高的两个细分赛道。

### 第 2 步：锁定主题类型

读 `references/style-dna.md` 的 5 个主题子模板，按用户输入归类：

- **A 妆面展示**：用户聚焦"某种妆"——首选
- **B 人设 vlog**：用户提到"一日 / 实录 / 月入 / 收入"
- **C 招生引流**：用户提到"课 / 学员 / 招生 / 周末班"
- **D 客户改造**：用户提到"客户 / 前后 / 改造 / 换头"
- **E 情绪短文**：用户表达情绪（"好爽 / 太牛 / 夯了"）

### 第 3 步：建立人物锚（**关键步骤**）

> 这一步直接决定 3 张图能不能"看起来是同一个人"。务必认真。

读 `references/persona-anchor.md` 选定锚定模式：

**模式 1：用户提供参考图**
- 用 Read 工具读图
- 按 `persona-anchor.md` 的 15 项特征清单逐项填写
- 输出一张「人物锁卡（Persona Lock Card）」

**模式 2：用户没提供图**
- 从 `references/persona-anchor.md` 的"内置人物库"5 个虚拟人物中选 1 个（或让用户选）
- 默认推荐：根据"风格主线"匹配最合适的（韩系→单眼皮淡颜25 / 日杂→方圆脸22 / 千金→长脸28）

**模式 3：用户已有自己的"模特库"**
- 用户在 `assets/custom-personas/` 下放图，并在请求里提及"用我的 X 号模特"
- 按用户描述的特征 + 图片做锚

无论哪种模式，最终输出都是一张统一格式的「人物锁卡」，3 张图共享。

### 第 4 步：生成 3 张图的提示词（**JSON 结构化中文 · 面向 GPT-image-2 / Nano Banana 2**）

读 `references/image-prompts.md`，3 张图按以下分工：

| 图 | 角色 | 内容焦点 |
|---|---|---|
| **图 1 主图 / 封面** | 钩流量 | 模特 45° 侧脸特写，妆面整体氛围 |
| **图 2 细节** | 种草 | 眼妆 or 唇妆 or 局部特写，传递技术感 |
| **图 3 对比 / 场景** | 验证 | 妆前妆后对比 OR 模特上身/场景出片 |

**每张图都是一个 JSON 代码块**，包含以下字段（中文 key，便于用户自己改）：
- `图片编号` / `目标平台` / `画面比例: 竖向 2:3`
- `人物锁`：完整复制 Persona Lock Card 的 14 项特征（**不要省略写"同图 1"**）
- 图 2 / 图 3 额外加 `人物一致性指令` 字段，明确"使用与图 1 完全相同的模特"
- `姿态` / `妆容` / `灯光` / `背景` / `构图` / `风格` / `画质标签` / `禁止出现`
- **`禁止出现` 字段必须含前 5 条防图上加字约束**：① 图片上叠加任何文字 ② Emoji 贴纸 ③ 字幕/标题/caption ④ logo/品牌名/水印/签名 ⑤ AI 自动添加的注释——GPT-image-2 / Nano Banana 2 默认倾向加这些装饰元素，必须明确禁止

**严格不要混入**：
- ❌ Midjourney 的 `--ar 2:3` `--cref` `--cw` `--seed` `--style raw` 参数
- ❌ 英文段落自然语言（GPT-image-2 / Nano Banana 2 对结构化中文 JSON 的理解更稳）
- ❌ 在多张图之间用"同图 1"省略人物锁（模型对省略不稳定，必须每张图完整重复 14 项）

**人物一致性策略**（按目标平台选）：
- **Nano Banana 2**：一次性 prompt 同时给 3 段 JSON + 前置指令"生成 3 张同模特图片"
- **GPT-image-2**：同一对话分 3 次粘 JSON，第 2/3 次开头加"使用上方图片中的同一位模特"
- **有参考图时**：把参考图上传给工具，每张图 JSON 加 `参考图说明` 字段

### 第 5 步：生成 3 个版本的标题

读 `references/title-formulas.md`，从 5 个公式里选 3 个最匹配主题的：

| 主题类型 | 推荐公式组合 |
|---|---|
| A 妆面展示 | A 服务卡 + C 反问 + E 体感动词 |
| B 人设 vlog | D 数字身份 + A 服务卡 + C 反问 |
| C 招生引流 | A 服务卡 + D 数字身份 + E 体感动词 |
| D 客户改造 | A 服务卡 + B 体感 + C 反问 |
| E 情绪短文 | E 体感动词 + C 反问 + B 体感 |

每条标题 12-22 字，至少 1 个 Emoji（从 ‼️✨💕🔥💖🫧🎀💐🌸🩵🐈⁉️📍 选）。

### 第 6 步：生成 3 个版本的正文

读 `references/body-templates.md`，三个版本：

| 版本 | 字数 | 适用 |
|---|---|---|
| **精简版** | 80-100 字 | 快读用户 / 视频笔记 |
| **标准版** | 110-130 字 | 默认推荐 / 图文笔记 |
| **深度版** | 140-160 字 | 招生类 / 教学类 |

每版必含：客户故事/灵感 + 技术解读 + 价格/约妆信号 + 3-5 个 hashtag。

### 第 7 步：组装输出

按 `references/output-format.md` 的标准格式输出，包括：
1. 元信息块（主题分类 / 人物锁卡 / 风格细分）
2. 3 张图提示词（标 1/2/3，附人物一致性说明）
3. 3 版标题（标 1/2/3，附公式说明 + 字数）
4. 3 版正文（标 精简/标准/深度，附字数）
5. 发布前自检清单（5 项）

### 第 8 步：等待用户反馈

发完后**不要追加多余的话**。如果用户说"图 2 换个角度"或"标题再野一点"，就只改那一项，不要重做全套。

## References 索引

| 文件 | 何时读 |
|---|---|
| `references/style-dna.md` | 第 2 步——主题分类时；不确定风格主线时也要读 |
| `references/persona-anchor.md` | 第 3 步——建立人物锚时**必读**；含内置 5 人物库 |
| `references/title-formulas.md` | 第 5 步——选标题公式时 |
| `references/body-templates.md` | 第 6 步——写正文时 |
| `references/image-prompts.md` | 第 4 步——生成 3 张图提示词时**必读**；含一致性锁定语法 |
| `references/output-format.md` | 第 7 步——组装最终输出时 |

## 与"花里胡哨的小搓澡巾"博主的 4 个关键差异（**必须遵守**）

| 维度 | 原博主（南京） | 模仿账号（都匀） |
|---|---|---|
| **正文** | 100% hashtag 堆砌 | 80-160 字真实化叙事（修复短板） |
| **地域** | 南京 | 都匀（可扩黔南/贵阳/独山/三都/龙里/罗甸/贵定/平塘/惠水） |
| **工作室** | OUOU 化妆室 | 桃九九化妆室 |
| **风格主线** | 韩/日/欧美/美式/泰式全做 | 聚焦"日杂 + 韩系亚裔"两条主线（黔南三四线本地审美适配） |

## 输出风格

不要罗嗦，不要解释为什么这么做。直接产出三件套就完事。用户如果想理解原理，会单独问。

如果用户输入信息严重不足（连日期和想法都没有），**先用合理默认值产出一版**，然后在末尾用一行小字问"想换哪个维度"。不要先连珠炮提问让用户填表。
